{
  "name": "@webqam/vue-match-media",
  "version": "1.0.0",
  "description": "React to media query changes in your Vue application (useful for adaptive design).",
  "main": "index.js",
  "scripts": {
    "prettier": "prettier \"**/*.js\" \"**/*.json\" \"**/*.md\"",
    "prettier:check": "npm run prettier -- --check",
    "prettier:fix": "npm run prettier -- --write",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Webqam",
  "license": "MIT",
  "devDependencies": {
    "husky": "^4.2.1",
    "lint-staged": "^10.0.2",
    "prettier": "^1.19.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,json,md}": [
      "prettier --write"
    ]
  }
}
